name: Multi Schedule
on:
  schedule:
    - cron: '0 6 * * 1-5'
    - cron: '0 12 * * 0'

jobs:
  weekday-check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "weekday"
  weekend-report:
    runs-on: ubuntu-latest
    steps:
      - run: echo "weekend"
